#include<stdio.h>

int main()
{
    int a,b;
    a=3;
    b=5;

   //(bitwise AND)
    printf("a&b=%d\n",a&b);

   //(bitwise OR)
    printf("a|b=%d\n",a|b);

 //(bitwise XOR)
    printf("a^b=%d\n",a^b);

    return 0;
}